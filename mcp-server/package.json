{
  "name": "review-cards-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Servidor MCP para criar/gerenciar tópicos de estudo do Review Cards Pro a partir de um cliente de IA (Claude Desktop, Claude Code, etc).",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
